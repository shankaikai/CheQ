<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cheq">
    <!-- Enables internet access -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Users.RestaurantPageActivity"></activity>
        <!-- SplashScreen -->
        <activity
            android:name=".Login.SplashScreen"
            android:noHistory="true"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- LoginActivity -->
        <activity
            android:name=".Login.LoginActivity"
            android:noHistory="true"
            android:theme="@style/AppTheme" /> <!-- RegistrationActivity -->
        <activity
            android:name=".Login.RegistrationActivity"
            android:noHistory="true" /> <!-- PasswordActivity -->
        <activity
            android:name=".Login.PasswordActivity"
            android:noHistory="true" /> <!-- MainActivity -->
        <activity android:name=".MainActivity" /> <!-- DoneActivity -->
        <activity
            android:name=".Login.DoneActivity"
            android:noHistory="true" /> <!-- RestaurantActivity -->
        <activity
            android:name=".Restaurant.RestaurantActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>